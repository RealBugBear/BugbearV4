import 'package:bugbear_app/features/training/models/session_state.dart';

/// Service für Kalender-Integration.
/// Nutzt das SessionState-Modell, daher Import nötig.
class CalendarService {
  /// Legt einen Termin im lokalen Kalender oder via API an.
  Future<void> addSessionEvent(SessionState session) async {
    // TODO: Implementieren Sie hier das Anlegen eines Kalendereintrags
    // z.B. mit device_calendar oder einer REST-API.
  }
}
